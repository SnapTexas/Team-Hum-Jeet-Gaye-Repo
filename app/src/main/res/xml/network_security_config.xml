<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Health Tracker
    
    This configuration enforces:
    - TLS connections only (no cleartext)
    - System certificate authorities
    
    NOTE: Certificate pinning is disabled for development.
    For production, add proper certificate pins using:
    openssl s_client -connect firebaseio.com:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64
-->
<network-security-config>
    <!-- Base configuration for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Debug configuration (only for debug builds) -->
    <!-- This allows localhost connections for development -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
